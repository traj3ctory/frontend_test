import {
    PROJECT_TITLE,
    ClientToken,
    ClientDetail,
    expiryTime
} from './Constant';
import {
    storeValue, getValue, removeValue
} from './Storage';

export {
    PROJECT_TITLE,
    ClientToken,
    ClientDetail,
    expiryTime,
    storeValue,
    getValue,
    removeValue,
}