export interface LoginClientI {
    university_email: string;
    password: string;
  }

export interface RegisterClientI {
  university_email: string;
  first_name: string;
  last_name: string;
  grad_year: string;
}